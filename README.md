Pocket Pandit — React CDN demo

Quick start:

1. Serve repository root on a local HTTP server (browsers block fetch from file://).

```bash
# from /workspaces/Pocket-Pandit
python3 -m http.server 8000
# then open http://localhost:8000/templates/react/index.html
```

This demo uses React from CDN and loads `data/example_pujas.json`.
